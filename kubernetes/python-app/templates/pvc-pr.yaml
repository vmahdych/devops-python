apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azurefile-production
  namespace: production
  #namespace: {{ default "staging" .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-csi
  volumeName: azurefile
  resources:
    requests:
      storage: 3Gi
